HEADER:
	32 byte buffer identifier (hash, of whole buffer?)
	16 byte time stamp (not actual time, just a number that is incremented every time a packet is sent.)
	16 byte packet number (2^16 * 1024 = max bytes)
